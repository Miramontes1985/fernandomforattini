# Fernando M. Forattini Website

Astro landing page for `fernandomforattini.com`.

## Local Development

```sh
npm install
npm run dev
```

The local server runs at `http://127.0.0.1:4321/` by default.

## Build

```sh
npm run build
```

The static site is generated in `dist/`.
